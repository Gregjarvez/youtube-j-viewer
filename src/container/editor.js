import React from 'react';import {connect} from 'react-redux';import PropTypes from 'prop-types';import AceEditor from 'react-ace';import 'brace/mode/json';import 'brace/theme/textmate';import {setJson, formatJson} from '../redux/actions/editor';const Editor = ({json, setJsonToStore, formatJson}) => {  return (      <div className="layout">        <div className="layout--setting">          <button className="layout--text" onClick={() => formatJson(json)}>Format</button>          <button className="layout--text">Process</button>        </div>        <AceEditor            width="99%"            height="600px"            fontSize="13px"            mode="json"            theme="textmate"            value={json}            onChange={(json) => setJsonToStore(json)}            showPrintMargin={false}            focus            wrapEnable            highlightActiveLine        />      </div>  );};Editor.propTypes = {  json: PropTypes.string,  saveJsonToStore: PropTypes.func,  formatJson: PropTypes.func};const mapStateToProps = ({editor: json}) => ({  json,});const mapDispatchToProp = (dispatch) => ({  setJsonToStore: (json) => dispatch(setJson(json)),  formatJson: (json) => dispatch(formatJson(json))});export default connect(mapStateToProps, mapDispatchToProp)(Editor);